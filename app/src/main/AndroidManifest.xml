<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.fyp_fitledger">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!--<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />-->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/fit_ledger_logo_circle"
        android:label="@string/app_name"
        android:roundIcon="@drawable/fit_ledger_logo_circle"
        android:supportsRtl="true"
        android:theme="@style/Theme.FYP_FitLedger"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/display_app_name"
            android:theme="@style/Theme.FYP_FitLedger"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DemographicActivity"
            android:theme="@style/Theme.FYP_FitLedger" />
        <activity android:name=".HomeActivity" />

        <activity android:name=".DietActivity"/>
        <activity android:name=".DietLogActivity"/>
        <activity android:name=".FoodListActivity"/>

        <activity android:name=".WorkoutActivity"/>
        <activity android:name=".ExerciseListActivity"/>
        <activity android:name=".ExerciseDetailActivity"/>
        <activity
            android:name=".WorkoutLogActivity"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan"/>

        <activity android:name=".MoreActivity"/>

        <meta-data
            android:name="com.google.android.gms.auth.api.identity.SIGN_IN_OPTIONS"
            android:value="DEFAULT_SIGN_IN" />
        <meta-data
            android:name="com.google.android.gms.auth.api.credentials.WebClientId"
            android:value="@string/web_client_id" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>